kind          = "linter"
name          = "cpplint"
family        = "cpplint"
extensions    = [".cpp", ".cc", ".cxx", ".c++", ".c", ".h", ".hpp", ".hxx"]

[version]
argument = "--version"
regex    = 'cpplint (\d+\.\d+\.\d+)'

[run]
check = ""
fix   = ""

[settings]
filter     = "--filter={value}"
linelength = "--linelength={value}"
root       = "--root={value}"
