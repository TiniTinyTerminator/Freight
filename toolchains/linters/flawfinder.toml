kind          = "linter"
name          = "flawfinder"
family        = "flawfinder"
extensions    = [".cpp", ".cc", ".cxx", ".c++", ".c", ".h", ".hpp", ".hxx"]

[version]
argument = "--version"
regex    = 'Flawfinder version (\d+\.\d+)'

[run]
check = "--error-level=1"
fix   = "--error-level=1"

[settings]
minlevel = "--minlevel={value}"

[values]
minlevel = ["0", "1", "2", "3", "4", "5"]
