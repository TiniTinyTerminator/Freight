[package]
name        = "greeter"
version     = "0.1.0"
description = "A tiny greeting library with a FANCY compile-time switch"

[language.cpp]
std = "c++17"

[lib]
type = "static"
srcs = ["src/greeter.cpp"]
hdrs = ["src/greeter.h"]
