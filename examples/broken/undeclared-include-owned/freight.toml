[package]
name        = "undeclared-include-owned"
version     = "0.1.0"
description = "Declares zlib (which owns <zlib.h>) but not the provider of <sqlite3.h> — only the latter is flagged under deny."

[language.c]
std = "c17"

[[bin]]
name = "undeclared-include-owned"
src  = "src/main.c"

[dependencies]
zlib = "1.0"

[lints]
undeclared-include = "deny"
